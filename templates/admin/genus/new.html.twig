{% extends "admin/base.html.twig" %}

{% block title %}New Genus{% endblock%}
{% block bodyTitle %}New Genus{% endblock%}

{% block body %}

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title">{{ "app.admin.crud.new"|trans }} {{ "app.admin.crud.genus"|trans }}</h5>
                </div>
                <div class="card-body">
                    {{ include('admin/genus/_form.html.twig') }}
                </div>
                <div class="card-footer d-flex justify-content-end">
                    <a class="btn btn-secondary" href="{{ path('app_admin_genus_index') }}">{{ "app.admin.crud.backtolist"|trans }}</a>
                </div>
            </div>

        </div>
    </div>

{% endblock %}
