<form method="post" action="{{ path('app_admin_genus_delete', {'id': genu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ genu.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
