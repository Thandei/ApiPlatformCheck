{% extends 'admin/base.html.twig' %}

{% block title %}Edit Locale{% endblock %}
{% block bodyTitle %}Edit Locale{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title">Edit Locale</h5>
                </div>
                <div class="card-body">
                    {{ include('admin/locale/_form.html.twig', {'button_label': 'Update'}) }}
                </div>
                <div class="card-footer d-flex justify-content-between">
                    <a class="btn btn-secondary" href="{{ path('app_admin_locale_index') }}">back to list</a>
                    {{ include('admin/locale/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}
