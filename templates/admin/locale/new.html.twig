{% extends 'admin/base.html.twig' %}

{% block title %}Create New Locale{% endblock %}
{% block bodyTitle %}Create New Locale{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title">Create New Locale</h5>
                </div>
                <div class="card-body">
                    {{ include('admin/locale/_form.html.twig') }}
                </div>
                <div class="card-footer d-flex justify-content-end">
                    <a class="btn btn-secondary" href="{{ path('app_admin_locale_index') }}">back to list</a>
                </div>
            </div>
        </div>
    </div>

{% endblock %}