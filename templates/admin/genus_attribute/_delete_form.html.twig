<form method="post" action="{{ path('app_admin_genus_attribute_delete', {'id': genus_attribute.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ genus_attribute.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
