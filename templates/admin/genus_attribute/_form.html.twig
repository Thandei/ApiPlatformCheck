{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="d-flex justify-content-end">
        <button class="btn btn-primary">{{ button_label|default('app.admin.crud.save'|trans) }}</button>
    </div>
{{ form_end(form) }}
