{% extends "admin/base.html.twig" %}

{% block title %}{{ "app.admin.crud.userdetails"|trans }}{% endblock %}
{% block bodyTitle %}{{ "app.admin.crud.userdetails"|trans }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <h5 class="card-title">{{ "app.admin.crud.userdetails"|trans }}</h5>
                </div>
                <div class="card-body">
                    <div class="table-responsive dt-responsive">
                        <table class="table table-bordered">
                            <tbody>
                            <tr>
                                <th>{{ "app.admin.crud.id"|trans|caseupper }}</th>
                                <td>{{ user.id }}</td>
                            </tr>
                            <tr>
                                <th>{{ "app.admin.crud.email"|trans|caseupper }}</th>
                                <td>{{ user.email }}</td>
                            </tr>
                            <tr>
                                <th>{{ "app.admin.crud.roles"|trans|caseupper }}</th>

                                <td>
                                    {% for role in user.roles %}
                                        {{ role }}
                                    {% endfor %}
                                </td>
                            </tr>
                            <tr>
                                <th>{{ "app.admin.crud.account"|trans|caseupper }} {{ "app.admin.crud.name"|trans|caseupper }}</th>

                                <td>
                                    {{ user.accountname }}
                                </td>
                            </tr>
                            <tr>
                                <th>{{ "app.admin.crud.nickname"|trans|caseupper }}</th>

                                <td>
                                    {{ user.nickname }}
                                </td>
                            </tr>
                            <tr>
                                <th>{{ "app.admin.crud.createdat"|trans|caseupper }}</th>
                                <td>{{ user.createdAt ? user.createdAt|date(shared.defaultDatetimeFormat) : '' }}</td>
                            </tr>
                            <tr>
                                <th>{{ "app.admin.crud.approvalbadge"|trans|caseupper }}</th>
                                <td>{{ user.approvalbadge ? '<i class="ti ti-checks fw-semibold text-success"></i>' : '--' }}</td>
                            </tr>
                            <tr>
                                <th>{{ "app.admin.crud.business"|trans|caseupper }}</th>
                                <td>{{ user.hasbusiness ? '<i class="ti ti-checks fw-semibold text-success"></i>' : '--' }}</td>
                            </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="card-footer d-flex justify-content-between">
                    <a class="btn btn-secondary" href="{{ path('app_admin_user_index') }}">{{ "app.admin.crud.backtolist"|trans }}</a>
                    <a class="btn btn-primary" href="{{ path('app_admin_user_edit', {'id': user.id}) }}">{{ "app.admin.crud.edit"|trans }}</a>
                    {{ include('admin/user/_delete_form.html.twig') }}
                </div>
            </div>

        </div>
    </div>

{% endblock %}